Playlist for learning JavaScript is found here:
    https://www.youtube.com/playlist?list=PL0Zuz27SZ-6Oi6xNtL_fwCrwpuqylMsgT


Javascript Quick Start:

    Javascript comments are denoted by double slash //

    Datatypes:
        String denoted by quotes: ""
        number
        boolean
        undefined

    Keyword 'let' declares a new variable
    Keyword 'typeof' gives the type of a value

    Variables are not statically typed. The type of a variable can be changed dynamically throughout a program

    The + operator concatenates string types

    Booolean operators:
        Equality: ===
        Inequality: !==
        Greater or equal to: >=
        Less or equal: <=
        Greater: >
        Less: <

    The alert() function prints an alert box to the page with whatever is passed to it

Lesson 1:

    The console.log() function prints its arguments to the console

Lesson 2:
    Keyword 'const' declares a variable that cannot be reassigned

    String methods:
        string.length of a string provides the length of the string
        string.charAt() function returns the character at the index given as an arguments
            0 indexed
        string.indexOf() function returns the first occurence of a substring within the string
        string.lasIndexOf() function returns the last occurence of an arugment substring
        
        string.slice(x, y) function returns the substring between positions x and y within string
            if y >= x, nothing is returned
            Does not return the character at index y
                if y = x + 1, only the character at index x is returned
        string.slice(x) function returns the substring beginning from position x to the end of the string
        
        string.toUpperCase() function returns string in all uppercase
        string.toLowerCase() function returns string in all lowercase

        string.includes("string") function returns a boolean of whether "string" is equal to
            or is a substring of string

        string.split() function returns an array of strings which are substrings of string split at the position of an argument character
            Removes the argument character from the resulting strings
                i.e. Mathematic.split("e") returns ["Math", "matics"]
            An argument of empty quotes , "", returns an array where each element is a letter in string
                i.e. Toy.split("") returns ["T", "o", "y"]
            
            A webpage of all string methods available for JavaScript can be found here:
                https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String
            Usefule String methods can be found here:
                https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/Scripting/Useful_string_methods

Lesson 3:
    Integers and floats are number datatypes

    A comparison between an int and float is false if the fractional part of the float is not equal to the int
        i.e. 42 === 42.0 but 42 !== 42.01

    A string can be cast to a number with the Number() function
    String and underfined data passed to the Number() function returns NaN
        NaN means "Not a Number"
    Booleans passed to Number() will return 1 if true or 0 if false

    Number methods:
        Number.isInteger() function returns a boolean whether the argument is an integer
        Number.parseFloat() function returns a floating point value equal to its argument
            If the argument cannot be parged, return NaN
        Number.parseInt() function removes non numeric characters
            Converts non number datatypes to number
        Number.toFixed() function limits decimal length to the number of decimals given as an argument
            Rounds up when removing trailing decimals
            Returns as a string
        Number.toString() converts a number to a string
        Number.isNaN() function returns boolean if value is NaN AND is a number
            A global isNaN() function returns if the value is NaN only

Lesson 4:
    This lesson covers JS Math object. A complete list of fields and methods can be found here:
        https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math

    Useful Math object methods:
        Math.trunc() function returns a truncated argument
        Math.round() function returns the argument rounded to the nearest integer
        Math.ceil() function returns the next largest integer from the argument
        Math.floor() function returns the next smallest integer from the argument
        Math.pow(x, y) function returns x raised to the power y
        Math.min() function returns the smallest value in a list of numbers
        Math.max() function returns the largest value in a list of numbers
        Math.random() function returns a random value in the range [0, 1)
            0 inclusive, 1 exclusive

Lesson 5:
    Coding challenge: Write code that will return a random letter from my name in main.js

Lesson 6:
    If statements
    
    undefined values evaluate to false

Lesson 7:
    Switch statements

Lesson 8:
    The ternary operator is a conditional operator
    syntax: condition ? ifTrue : ifFalse;

Lesson 9:
    User input
    The confirm() function returns true on an 'ok' input and false on a 'cancel' input
    The prompt() function returns a string input if a value is input or null if nothing is entered and 'cancel' is clicked
    The nullish coalescing operator '??' handles null inputs.
        Acts as a conditional
        i.e.
            let name = prompt();
            console.log(name ?? "value returned if 'name' is null");
    
    string.trim() trims whitespace

Lesson 10:
    A simple rock, paper, scissors game
    
    location.reload() reloads the page

Lesson 11:
    For loops, while loops, and do while loops

    While structure:
        while (condition) {
            loop body
        }

    Do while structure:
        do {
            loop body
        } while (condition)

        in a do while loop occurs at least once

    For structure:
        for (index value initialization; condition; index increment) {
            loop body
        }

Lesson 12:
    Functions:
        Declaration syntax:
            function functionName(parameterList) {
                function body
            }

        If an argument is missing from the function call, that argument is passed
            to the function as undefined. In the case of the sum() function
            in the accompanying main.js file in the Lesson 12 folder, the function will return NaN
            unless handled properly.

    typeof keyword prints the return type of a function

        Arrow function declaration syntax:
            variable = (parameterList) => {
                functionBody;
            }

Lesson 13:
    Comparing keywords const, let, var, and scope

    All initilize variables

    var responds differently from let and const
        var can allow two variables to have the same name
        However, var and let allow reassignment, const does not

        Use const whenever possible otherwise, use let when a variable will need to be reassigned.
        
        Block scope is a type of general local scope in JS where a variable is scoped to a code block not tied to a function or loop
            So function, loop, and conditional scopes are all block local scope, but brace defined blocks of code do not need to be tied to any of these

        If var is used to re/assign a variable inside of a block within a function, it is function scoped
            const and let is block scoped

Lesson 14:
    Arrays
        declaration:
            const arrayName = [];
        
        Elements in arrays do not need to be of the same type?
        Arrays, like strings, have a length property.
            This is likely because strings are arrays of characters.

        Arrays are zero indexed.

        Add elements to an array with the push() function
        Remove elements from an array with the pop() function
            Pop returns the item that gets removed
        Add elements to the front of an array with the unshift() function
            unshift returns the new length of the array
        Remove an element from the front of an array with the shift() function
            shift returns the item that gets removed

        This generally should not be used, but one way to delete an element is through the delete keyword
            delete array[index]
        This way removes the element but does not shift the remaining elements into new indices as expected

        The array.splice(startIndex, deleteCount, elementsToAdd[]) method deletes a number of elements from a starting index
            splice can also replce elements with new ones
            If no items are to be removed, numberToRemove = 0
                If you want to add elements without removal, new elements get added before startIndex

        Other array methods:
            slice() behaves the same as for strings
            reverse() reverses the element order in the array
            join() creates a comma separated string of all the elements in the array
            concat() concatenates the argument array to the end of the calling array
                The spread operator: '...' is a new way to concat two or more arrays:
                    const newArr = [...array1, ...array2]

Lesson 15:
    Refactoring rock, paper, scissors to first use a while loop and arrays and then with functions